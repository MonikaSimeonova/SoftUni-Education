function join(first, second, delimeter){
    console.log(`${first}${delimeter}${second}`)
    //or
    let result = first+delimeter+second
    console.log(result)
}
join('John',
'Smith'
,

'->')

//Write a function, which receives two names as string parameters and a delimiter. Print the names joined by the delimiter.