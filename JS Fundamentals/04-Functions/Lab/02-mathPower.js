function mathPower(num, numToRaised){

    let res = Math.pow(num, numToRaised)
    // num ** numToraised (sushtoto e)
    console.log(res);
}
mathPower(2,8)

/*Write a function that calculates and print the value of a number raised to a given power */