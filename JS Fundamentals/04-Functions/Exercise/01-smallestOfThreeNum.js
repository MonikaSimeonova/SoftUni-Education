function smallestFromThreeNum(firstNum,secondNum,lastNum){

    let smallestNum = Math.min(firstNum,secondNum,lastNum);
    console.log(smallestNum);

}
smallestFromThreeNum(2,3,5)

/*Write a function that receives three integers and prints the smallest number. Use an appropriate name for the function. */